--- bochs-2.6.1/configure.in.autofoo~	2013-04-27 14:34:14.106828483 +0200
+++ bochs-2.6.1/configure.in	2013-04-27 14:36:45.815489239 +0200
@@ -5,6 +5,9 @@ AC_INIT(bochs.h)
 AC_REVISION([[$Id: configure.in 11665 2013-04-07 15:27:37Z vruppert $]])
 AC_CONFIG_HEADER(config.h)
 AC_CONFIG_HEADER(ltdlconf.h)
+AC_INIT_AUTOMAKE([1.13 foreign no-define])
+AC_REQUIRE_AUX_FILE([install-sh])
+AC_REQUIRE_AUX_FILE([mkinstalldirs])
 
 dnl // Put Bochs version information right here so that it gets substituted
 dnl // into all the right places.
@@ -20,6 +23,7 @@ changequote(<<, >>)
 changequote([, ])
 
 dnl Detect host and target
+AC_CANONICAL_BUILD
 AC_CANONICAL_HOST
 AC_CANONICAL_TARGET
 
@@ -125,7 +129,7 @@ if test "$MSVC_TARGET" != 1; then
   dnl Check for dlopen support
   AC_LIBTOOL_DLOPEN
   dnl Configure libtool
-  AC_PROG_LIBTOOL
+  LT_INIT
   dnl Configure the ltdl library.  This must go after AC_PROG_LIBTOOL or
   dnl else it disables shared libraries somehow.
   AC_LIB_LTDL
